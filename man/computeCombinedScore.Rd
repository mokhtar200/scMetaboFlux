% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_scoring.R
\name{computeCombinedScore}
\alias{computeCombinedScore}
\title{Compute Combined Pathway Score}
\usage{
computeCombinedScore(
  seurat_obj,
  score_cols,
  weights = NULL,
  name = "combined_score",
  method = c("mean", "sum", "weighted_sum", "geometric_mean")
)
}
\arguments{
\item{seurat_obj}{Seurat object with pathway scores in meta.data}

\item{score_cols}{Character vector of score column names to combine}

\item{weights}{Optional numeric weights for each score (must sum to 1 or same length as score_cols)}

\item{name}{Name for combined score column}

\item{method}{Combination method: "mean", "sum", "weighted_sum", "geometric_mean"}
}
\value{
Seurat object with combined score added to meta.data
}
\description{
Combine multiple pathway scores into a single metric using
weighted or unweighted combinations.
}
\examples{
\dontrun{
seurat_obj <- computeCombinedScore(seurat_obj, 
  score_cols = c("glycolysis", "tca_cycle", "oxidative_phosphorylation"),
  weights = c(0.3, 0.3, 0.4),
  method = "weighted_sum")
}
}
