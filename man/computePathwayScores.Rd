% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_scoring.R
\name{computePathwayScores}
\alias{computePathwayScores}
\title{Compute Pathway Activity Scores}
\usage{
computePathwayScores(
  seurat_obj,
  gene_sets = NULL,
  method = c("AUCell", "GSVA", "mean", "weighted_mean", "single_sample"),
  aucell_nbin = 24,
  gsva_k = 100,
  gsva_tau = 0.25,
  parallel_cores = 1,
  assay = NULL,
  slot = "data",
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{Seurat object with expression data}

\item{gene_sets}{Named list of gene sets (pathway genes). If NULL, uses
all predefined metabolicGeneSets.}

\item{method}{Scoring method: "AUCell", "GSVA", "mean", "weighted_mean", "single_sample"}

\item{aucell_nbin}{AUCell: Number of bins for AUC calculation (default 24)}

\item{gsva_k}{GSVA: Number of genes for kernel width (default 100)}

\item{gsva_tau}{GSVA: Tau parameter for gene set scoring (default 0.25)}

\item{parallel_cores}{Number of cores for parallel computation (default 1)}

\item{assay}{Assay name to use (default active assay)}

\item{slot}{Slot to use for expression data (default "data")}

\item{verbose}{Print progress messages}
}
\value{
Seurat object with pathway scores added to meta.data
Each score is named according to the pathway (e.g., "glycolysis", "oxidative_phosphorylation")
}
\description{
Calculate metabolic pathway activity scores for each cell using
various enrichment methods including AUCell, GSVA, and mean expression.
}
\examples{
\dontrun{
seurat_obj <- computePathwayScores(seurat_obj, 
  gene_sets = metabolicGeneSets[c("glycolysis", "tca_cycle", 
  "oxidative_phosphorylation")],
  method = "AUCell")
}

\dontrun{
seurat_obj <- computePathwayScores(seurat_obj, 
  method = "mean",
  verbose = TRUE)
}
}
